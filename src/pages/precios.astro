---
/**
 * Ruta /precios (español). El slug lo declara `src/data/rutas.ts` y este
 * fichero tiene que llamarse igual que él: si uno de los dos cambia sin el
 * otro, el menú apunta a un 404. Toda la página vive en el componente.
 */
import Precios from '../components/paginas/precios.astro';
---

<Precios locale="es" />
