---
/**
 * Ruta /control (español). El nombre del fichero TIENE que coincidir con
 * `rutas['control'].es.slug` de `src/data/rutas.ts`, que es de donde el menú, el
 * selector ES/EN, el canonical y el sitemap sacan esta URL.
 */
import Control from '../components/paginas/control.astro';
---

<Control locale="es" />
