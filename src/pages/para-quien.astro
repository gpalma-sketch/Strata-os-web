---
/**
 * Ruta ES de la página «Casos». El nombre del fichero tiene que coincidir con
 * el slug que declara `rutas.casos.es.slug` en `src/data/rutas.ts` ('/para-quien'):
 * si dejan de coincidir, el menú apunta a un 404 y el build no avisa.
 */
import Casos from '../components/paginas/casos.astro';
---

<Casos locale="es" />
