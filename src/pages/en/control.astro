---
/**
 * Ruta /en/control (inglés). El slug coincide con el español —«control» es la
 * misma palabra en los dos idiomas— pero el título y la descripción no, y salen
 * del mapa dentro del componente. Ver `rutas['control'].en.slug`.
 */
import Control from '../../components/paginas/control.astro';
---

<Control locale="en" />
